/*my name:anfaljafar
/*class:computer
/*department:second
#include<iostream>
#include<stack>
#include<queue>
using namespace std;

void main()
{
  int linkedlist;
   cout<<"enter  6,8,9:;
     cin>>linkedlist;
     switch(linkedlist)
     
 }
 case 6:
 cout<<"linkedlist";
  break:
  case 8;
  cout<<stack";
  break:
  case 9;
  cout<<"queue";
  break:
}
};

int x;
 cin.exceptions
   
    (cin.faibit);
      try
      {
         cin>>x;
         
         cout<<x;
       }
        catch(...)
        {cerr<<this is not number"<<endl;

struct node{
      int
      char
        node*next;
    };
    typedef node
    *node_ptr;
       void firstnode
       (node_ptr&first,int add,int remove)
         {first==null)
          {
          first=(node*malloc(sizeof(node));
              first->add=t;
               strcpy(first->add,add);
               first->next=null;
           }
           else
           {
            cout<<"add"<<endl;
            cout<<"remove"<<endl;
        }
    };
    (node_ptr&q)
    {
      node_ptr p;
      p=q;
      while
           (p!=null)
            {
                p=p->next;
             }
             
           }
           
};
class stack{
  int top;
  int item[max_size];
public:
  stack():top(1){}
   
    void push(int element)
}

    item[top]=element
    {
        if(top)>=max_size 1)
          {
              cout<<"stack full on push";
            
        }
        else{
             top==;
              item[top]=element
                
                bool isempty()
                {
				
				}
				  return top<0;
				  {
				     void pop()
				 }
				 if (is empty())
				 cout<<"stack empty on pop"<<endl;
				 else
				  top--;
			}
			void pop (int & element))
			{
			if (isempty())
			 cout<<"stack empty on pop";
		}
		element=item [top]
		 top--;
	}
}
void gettop(int&stacktop)
 { 
   if(isempty))
     cout<<"stack empty on get top";
     else 
     {
        stack top=item[top]
        cout<<stack top<<endl;
      }
      void print()
      {
       for (size_t i+0;i<top+1;i++)
       {
        cout<<item[i]<<"";
    }
         cout<<"j";
         cout<<endl;
         
     }
 }
};
      int main()
         stack s;
        s.push(5);
		 s.push(10);
		  s.push(25);
		   s.push(20);
		   int y=0
		    s.gettop(y);
		     cout<<y<<endl;
(){queue <int>
 p; q.push(1)
 q.push(2)
 q.push(3)
   cout<<q.front()<<endl;q.pop
   cout<<q.front()<<endl;q.pop
     (); if (q.empty())
  }
  cout<<"qeue is empty";
}

